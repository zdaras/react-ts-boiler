import React from 'react';

import { Flex } from '@/styled/flex';

export const Home = () => (
	<Flex center>
		<Flex center sm={6} xs={8} padding="1rem">
			<span>HOME</span>
		</Flex>
	</Flex>
);

export default Home;
